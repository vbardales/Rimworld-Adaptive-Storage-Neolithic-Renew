# PickleTools research steps, French, launch 2 of 2, feature 10

2026-09-21, second launch of the same sequence as `../pickle-run-2026-09-21-pickletools-fr-08/`, one hold of the lock.

**`exitReason: passed`. 2 scenarios played of 2, 2 passed.**

- The storage tab and its project by their French labels: `Stockage` reaches `ASFAdaptiveStorage`, the record is labelled `Stockage`,
  and `Stockage néolithique` is listed at 400.
- The Main tab by its French label `Principal`, which reaches `Main`: the case where the label and the def name differ.

No capture.

Pass `avec-pickletools`: `-DepMap wsl-deps.avec-pickletools.map`, which stages the framework and the shared research steps of
`PickleTools/ResearchSteps` (packageId `nelim.pickletools.research`), 13 mods staged and all loaded. `-IncludeWip`, the features being
`@wip @pickletools`. Tree: commit `df12a10` plus the uncommitted files of the features 08 to 10, the map and the Keyed test data of the
companion (`Mod/Languages/*/Keyed/PickleTests.xml`). Copied from the archive of the next run; scenarios played = scenarios written.
