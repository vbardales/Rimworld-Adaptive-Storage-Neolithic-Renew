# 2026-09-23 — Full-DLC Russian pass, first attempt (`wsl-deps.map`, setName `map`, feature 07)

- `exitReason: failed`: 2 scenarios, 1 passed (granite and vacstone usable while Russian is active), 1 failed.
- Failure, a defect of the suite, the same as in French: Pickle's `def ... field ...` step refuses `ASNeolithicPlinthStone` (ThingDef and GraphicsDef share the name).
  The run staged the step assembly from before the fix. Feature 07 now uses the suite's own step, and takes one capture centred on the pot and one on the chunk stack.
- The old capture was centred between the two buildings and was not opened as evidence; replay needed: all of feature 07 in Russian.
- Raw report minified on disk: `tests/Pickle/Evidence/ru-full-2026-09-23/` (summary, junit, trimmed Player.log).
