# 2026-09-23 — Stones English pass (`wsl-deps.stones.map`, setName `stones`, feature 06)

- `exitReason: passed`, 2 of 2 scenarios passed: the shared stone buildings remain the only building defs (no per-stone def for `Kura_Andesite`, with the
  "does not exist" step now scanning the database), and a stone the mod never listed builds all three variants.
- Capture opened and accepted: the hover label reads "Andesite chunk plinth (normal)"; the large pot, the plinth and the chunk stack render in the third-party stone's colour.
- Counts as the `Kura.ExtraStone` play of feature 06 in English. Scenario 2 of feature 05 (French) is played by the stones French pass.
- Raw report minified on disk: `tests/Pickle/Evidence/stones-en-2026-09-23/` (summary, junit, trimmed Player.log, the capture as JPEG).
