# 2026-09-23 — Core-only English pass (watchdog timeout)

- Pass: `wsl-deps.core.map` (all five DLCs off), English, features 01-04 and 08 via `tests/Pickle/Run-CoreEnglish.ps1`.
- Result: `exitReason: watchdog-timeout`, launcher exit 3. 5 scenarios written, 5 passed (all of feature 01 up to the save load).
- Cause: the scenario "loading a game with the mod raises no error" loads the `test-colony` save, which references Royalty,
  Biotech and Odyssey defs (`Orbit`, `NobleApparel`, `Xenogermination`, ...). With the DLCs off the load never finishes and
  Pickle's 120 s watchdog kills the game. An environment limit of the fixture, not a defect of the mod.
- Consequence: the Core-only pass is not playable with this fixture and is not required by `AUDIT.md`. The English pass runs
  with every DLC (`wsl-deps.map`) instead.
- Raw report: deleted 2026-09-23 (its 5 scenarios are replayed by the full-DLC English pass; disk-space rule of the root AGENTS.md).
