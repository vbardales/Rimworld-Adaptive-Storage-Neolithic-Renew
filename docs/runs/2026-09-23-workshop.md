# 2026-09-23 — Workshop captures pass (`wsl-deps.workshop.map`, setName `workshop`, feature 11)

- `exitReason: passed`, 6 of 6 scenarios passed, nothing skipped (the Odyssey plinth scenario and the `[K]Extra Stone` scenario both ran). Feature 11 is `@requires:nelim.pickletools.screenshotstudio`.
- All six raw captures opened. Each scene renders on the screenshot studio's emblem as intended: the whole set with contents, three baskets (empty, one item, two items),
  chunk stacks in granite and marble, six large pots with different foods and a lidded one, wood/granite/vacstone plinths, and the andesite pot and stack beside granite.
  Same scenes as the six crops already in `Art/WorkshopScreenshots/` (2026-09-22 run): the crops stay valid. The subjects are small on the raw frame and the lidded pot hides its contents.
- Raw report minified on disk: `tests/Pickle/Evidence/workshop-2026-09-23/` (summary, junit, trimmed Player.log, six captures as JPEG).
