# 2026-09-23 — Full-DLC English pass, first attempt (`wsl-deps.map`, setName `map`)

- Feature 01: `exitReason: passed`, 7 of 7 passed (framework loads, nine buildings, GraphicsDefs, research projects and tab, save load without error).
- Feature 02: `exitReason: failed`, 2 of 4 passed, 2 failed. The failed launch ends the `-Then` sequence, so 03, 04, 08 and 12 were not played.
- Failures, both defects of the suite, not of the mod:
  - `def "ASNeolithicLargePotChunkGranite" does not exist` used `GetNamedSilentFail`, which also resolves the back-compat alias the
    save-migration patch registers for the old defName. The step now scans `DefDatabase.AllDefsListForReading`.
  - `blueprint for ASNeolithicLargePotStone made from ChunkGranite is not at (140, 155)` read `Thing.Stuff`, which is empty on a
    blueprint. The step now reads `EntityToBuildStuff()` and lists the blueprints found in its failure message.
  Neither fix has been replayed yet; the aliasing explanation is a diagnosis to confirm on the next run.
- The launcher also ended with a `Copy-Item ... DirectoryNotFoundException` while copying the evidence (long screenshot paths).
- Raw report minified on disk: `tests/Pickle/Evidence/full-dlc-en-2026-09-23/seq1` and `seq2` (summary, junit, trimmed Player.log).
