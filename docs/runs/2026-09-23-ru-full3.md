# 2026-09-23 — Full-DLC Russian pass, third try (`wsl-deps.map`, setName `map`, feature 07)

- `exitReason: passed`, 2 of 2 scenarios passed: the shared stone defs are translated in Russian (label of the pot, plinth, chunk stack and description of the chunk stack, read with the suite's own step), and granite and vacstone can be used while Russian is active.
  This replays `docs/runs/2026-09-23-ru-full.md` (failed on the ambiguous plinth name); the second try never played, its launcher stopped on a purged queue ticket (`Get-Item` on a missing `.ticket` file after about 8 hours of waiting).
- Both captures opened and accepted: the UI is Russian and the hover labels read "Большой горшок из granite chunk" and "Куча из vacstone chunk". As in French, the stone's own name keeps the English word "chunk" (label from the chunk def).
- Raw report minified on disk: `tests/Pickle/Evidence/ru-full3-2026-09-23/` (summary, junit, trimmed Player.log, two captures as JPEG). The first Russian attempt's raw folder was deleted, its summary stays in `docs/runs/2026-09-23-ru-full.md`.
